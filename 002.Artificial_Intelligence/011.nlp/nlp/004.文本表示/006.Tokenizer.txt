
把原文切成模型能吃的小块（token），
再编成数字 id。 和前面笔记里的「分词」是同类事，

但切法不同：

（1）jieba 分词：按「词」切，如 梅兰芳 / 大剧院，给词频、词性、传统 NER 用
（2）BERT/BGE 的 tokenizer：按「子词」切（WordPiece 等），如 剧院 可能再拆，不必先 jieba

分词是给人看的词；tokenizer 是给 Transformer/Embedding 用的输入转换器。

