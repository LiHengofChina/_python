	/**
		学出来的是 Wq、Wk、Wv 等矩阵；词与词之间的注意力权重是每次根据输入现算的，
		不是训练完固定存好的。
		流程：输入 → 算出 Q/K/V → 算关注度 → 加权 V → 得到新表示。
	*/

//=================================== Transformer 一层里有什么？
//=================================== Transformer 一层里有什么？

重复堆很多层，每层大致包括：

输入
  ↓
Self-Attention     ← 词与词互相关注
  ↓
Add & Norm         ← 残差连接 + 归一化（类似 ResNet 思路）
  ↓
Feed Forward (FFN) ← 全连接，进一步加工
  ↓
Add & Norm
  ↓
输出（传给下一层）

说明：Encoder / Decoder 各自都会堆很多这样的层；
	谁双向、谁只能看左边、用在 BERT 还是 GPT，见前面「整体结构 / 对比」笔记。
