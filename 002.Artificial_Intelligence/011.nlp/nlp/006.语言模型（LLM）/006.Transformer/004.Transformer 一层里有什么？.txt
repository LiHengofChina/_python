	/**
		学出来的是 Wq、Wk、Wv 等矩阵；词与词之间的注意力权重是每次根据输入现算的，
		不是训练完固定存好的。
		流程：输入 → 算出 Q/K/V → 算关注度 → 加权 V → 得到新表示。
	*/

//=================================== Transformer 一层里有什么？
//=================================== Transformer 一层里有什么？

重复堆很多层，每层大致包括：

输入
  ↓
Self-Attention     ← 词与词互相关注
  ↓
Add & Norm         ← 残差连接 + 归一化（类似 ResNet 思路）//残差结构，在 Transformer 里也有（
  ↓
Feed Forward (FFN) ← 全连接，进一步加工
  ↓
Add & Norm
  ↓
输出（传给下一层）



//=================================== Encoder（编码器）：读懂输入
//=================================== Encoder（编码器）：读懂输入

	把整句话看一遍，理解含义
	双向看上下文（左右都能看）
	典型：BERT
	用途：分类、理解、RAG 检索用的 Embedding


//=================================== Decoder（解码器）：生成输出
//=================================== Decoder（解码器）：生成输出

	根据已有内容，一个词一个词往外写
	只能看左边（不能偷看未来）
	典型：GPT、ChatGPT
	用途：对话、写作、续写

