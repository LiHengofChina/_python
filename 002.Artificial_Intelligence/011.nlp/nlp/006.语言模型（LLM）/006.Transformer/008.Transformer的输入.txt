
Transformer "训练和推理" 时都一样的输入构造，不只训练。
		/**
			训练：用数据调参数，让模型学会
			推理：参数固定，拿来预测/生成（日常用 ChatGPT 就是推理）
		*/

每个 token 进 Transformer 前：
最终输入 ≈ Embedding（语义）+ 位置编码（第几位）

//======================================================================= 注意：
//======================================================================= 注意：

"训练" 和 "推理" 进模型前都要有：

词（token）向量 + 位置信息


