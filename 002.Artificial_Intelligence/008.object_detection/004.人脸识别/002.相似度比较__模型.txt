

//======================================= 孪生网络（连体网络）
//======================================= 孪生网络（连体网络）

（1）两个一模一样的模型，同时 "共享一组权重" ，
		//所以是同一个模型不同的拷贝

（2）


（3）训练时，输入同一个类别，



//======================================= 三元神经网络
//======================================= 三元神经网络

	孪生网络 的变种网络

一个参考样本（也称为锚点样本），
一个同类样本（正样本），
一个异类样本（负样本）

（1）同类样本的距离越近越好，
（2）异类样本的距离越远越好。
