
//=================================== （1）SSH 执行命令 ssh_exec
//=================================== （1）SSH 执行命令 ssh_exec

（1）用途		 查磁盘、进程、日志
（2）参数		 connectionId / host, command
（3）注意		 只读命令 默认可用；rm、restart 要审批或禁止

"description": "在目标服务器执行只读 shell 命令，如 df -h、ps aux、tail 日志。禁止破坏性命令。"

//=================================== （2）RAG 检索 rag_search
//=================================== （2）RAG 检索 rag_search

（1）用途		 查运维手册、故障案例库	
（2）参数		 query（自然语言问题）
（3）实现		 day_29 的流程：切块 → 向量检索 → 返回 Top-K 文本
{
  "name": "rag_search",
  "description": "从运维手册和故障案例库检索相关文档，用于查标准处理流程",
  "parameters": {
    "query": { "type": "string", "description": "检索问题" }
  }
}

RAG 不是和 Tool 并列的另一套系统，而是注册成一个 Tool，供 Agent 在需要时调用。

//=================================== （3）查监控 query_metrics
//=================================== （3）查监控 query_metrics

（1）用途		CPU、内存、磁盘曲线
（2）参数		host, metric, timeRange

//=================================== 查日志 search_logs
//=================================== 查日志 search_logs

（1）用途		在日志系统搜 ERROR、关键字
（2）参数		host, keyword, from, to

//=================================== 发通知 send_alert
//=================================== 发通知 send_alert

（1）用途		通知值班人
（2）注意		敏感操作，建议需确认



