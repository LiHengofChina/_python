
//====================================
//====================================

工具让 模型可以调用JAVA方法

查磁盘、查手册、执行命令

迈进 ReAct 一步

Tool 让模型能调用你写的 Java 方法，

用来查磁盘、查手册、执行命令等；模型根据问题决定调哪个、传什么参数。

//==================================== 编写一个工具
//==================================== 编写一个工具

@Tool(description = "查询指定主机的磁盘使用情况...")
public String queryDiskUsage(
        @ToolParam(description = "主机标识") String hostId) {
    // 模拟返回
}

//==================================== 注册给模型：
//==================================== 注册给模型：

chatClientBuilder.defaultTools(opsTools).build();

