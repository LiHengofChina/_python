

//=================================================== 多个步骤、多轮推理怎么组织
//=================================================== 多个步骤、多轮推理怎么组织


（1）多个步骤、多轮推理怎么组织
（2）API 返回 每一步中间结果
（3）编排器按意图决定执行哪些命令
（4）初稿 → 复核 两轮 LLM 写清楚


//=================================================== 编排流程（5 步）
//=================================================== 编排流程（5 步）


Step 1  意图识别     → LLM 第 1 轮（DISK/MEMORY/...）
Step 2  RAG检索      → 代码显式调 vectorStore
Step 3  现场采集     → 编排器按意图执行 SSH 命令
Step 4  推理-初稿     → LLM 第 2 轮
Step 5  推理-复核     → LLM 第 3 轮（输出最终建议）


/**
调用 /api/orchestration/run 时，JSON 的 steps 数组会列出 5 步（order、name、output）。


一次性返回。

5 步在服务端按顺序全部跑完后，才把整个 JSON（含 steps 和 answer）一次返回。不是 SSE/WebSocket 逐步推送。

*/


//=================================================== 
//=================================================== 


http://localhost:8103/api/orchestration/run?q=linux-231磁盘快满了


http://localhost:8103/


能告诉我 linux-231 还剩下多少磁盘空间吗
