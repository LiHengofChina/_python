# 运维手册

## 磁盘空间告警处理

当 Linux 服务器磁盘使用率超过 85% 时，应优先排查 /var/log 目录。
常见步骤：
1. 使用 df -h 查看各挂载点使用率。
2. 使用 du -sh /var/log/* 找出占用最大的日志目录。
3. 对已归档的旧日志执行 logrotate 或手工压缩后移走。
4. 禁止直接 rm -rf 正在写入的日志文件，应 truncate 或重启对应服务后再清理。
