



//=================================================== 核心类
//=================================================== 核心类


//======== 自己起的名字、自己写的编排逻辑。
//======== 自己起的名字、自己写的编排逻辑。


IntentAnalyzer — 意图识别					//ChatClient

ManualRagRetriever — 显式 RAG			//VectorStore + SearchRequest

ToolExecutionPlanner — 按意图选命令		//自写 + LinuxSshExecutor（sshj）

OpsSynthesizer — 多轮推理（初稿 + 复核）	//ChatClient（调两次）

OpsOrchestrationService — 编排器入口		//纯 Java 编排，串上面几步






