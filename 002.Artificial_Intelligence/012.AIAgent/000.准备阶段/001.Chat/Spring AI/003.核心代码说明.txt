
//=============================================
//=============================================

chatClient
    .prompt()           // 开始构造 Prompt
    .user(userMessage)  // 用户消息
    .call()             // 同步调用 Ollama
    .content();         // 取回答文本


//============================================= 带系统角色（运维助手）：
//============================================= 带系统角色（运维助手）：
带系统角色（运维助手）：

chatClient.prompt()
    .system("你是银行运维助手...")
    .user(userMessage)
    .call()
    .content();

对应：Prompt = system + user。