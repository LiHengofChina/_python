
//============================================ 质量
//============================================ 质量

主要取决于：Embedding 模型、切块方式、检索 topK、大模型，和 Python/Java 关系不大。

（1）Python day_29：TF-IDF + Mock LLM → 质量偏低，适合学流程

（2）Java day_03：Ollama Embedding + 真 LLM → 质量明显更好，接近真实 RAG


同模型、同文档、同参数时，Python 和 Java 质量基本一致。

//============================================
//============================================

Embedding 指的是词嵌入，这里使用的是 Ollama Embedding 

用的是 Ollama 自带的预训练 "Embedding 模型"：
//============================================
//============================================

推荐时使用了 qwen2.5:7b 模型，
【检索（检查）】的时候使用了 " Ollama 自带的预训练 "Embedding 模型" nomic-embed-text //把文档块和问题变成向量，找相似内容
		//需要下载：ollama pull nomic-embed-text

【生成（回答）】使用了 qwen2.5:7b 模型 ：根据检索到的内容，组织成自然语言回答


//============================================ 实际流程：
//============================================ 实际流程：

（1）启动时把 ops_manual.txt 切块
（2）每块文本调用 nomic-embed-text → 得到向量
（3）存入 SimpleVectorStore（内存向量库）

（4）提问时，问题也编成向量，再算相似度检索


//============================================ 所以
//============================================ 所以

Embedding 模型负责「找资料」（快、专做语义匹配）
Chat 模型负责「写答案」（慢、擅长理解和生成）

