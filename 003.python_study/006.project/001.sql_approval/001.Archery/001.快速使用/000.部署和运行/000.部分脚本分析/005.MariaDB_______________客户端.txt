MariaDB






# 安装 nginx 和 mariadb-client
RUN apt-get install -yq --no-install-recommends nginx

# MariaDB 是一个数据库，
# MariaDB 是一个数据库，它是MySQL 的一个分支。暂时不安装它的客户端
# RUN apt-get install -yq --no-install-recommends nginx mariadb-client

