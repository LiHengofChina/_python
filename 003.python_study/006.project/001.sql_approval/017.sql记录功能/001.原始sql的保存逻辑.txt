


因为我使用了批量加入 数据，就是数据达到一定条数的时候才加入数据库，

但是呢 original_sql 表里面的数据是要先计算 sha-256 的值，
判断数据库中有没有，有则计数+1，没有才添加新记录
然后由于我是批量加入，这里我也需要判断批量的集合中是否有这个数据，
我的分析如下：


if(数据库不存在 && 集合不存在){ //说明是：新sql

	（1）次数为1
	（2）记录到集合  //后续批量加入

}else if(数据库不存在 && 集合存在)){//说明是：集合已经存在了,但是还没有添加到数据库

	（1）集合中的数据的：次数 + 1
	（2）这样多次的记录都会匹配到这条记录，而且能正确记录次数 //后续批量加入

}else if(数据库存在 && 集合不存在)){

	（1）删除数据库的内容，拿到它的次数
	（1）在原有次数上 + 1， 
	（2）这时数据库中数据，转移到集合中   //后续批量加入

}


按上面的逻辑，是不是可以正确保存 original_sql的数据，是不是同时也能提交效率呢


List<OriginalSql> originalSqlList = new ArrayList<>();//"原始sql"集合