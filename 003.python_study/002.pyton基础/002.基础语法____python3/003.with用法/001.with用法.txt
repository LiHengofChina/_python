
//================================================
//================================================
	with open('model.pickle','wb') as f :
		pickle.dump(model,f)

		//open() 打开文件并返回一个文件对象
		//as f  是将文件对象赋值给一个变量f的语法
		//with 语句 是上下文管理器的一种使用方式，
				//用于自动管理资源。在这种情况下，
				//文件对象会在with块结束时自动关闭，
				//无需手动调用f.close()方法。


