


	name[0]表示列表的第一个元素，
	name[-1]表示列表的最后一个元素，
	name[-2]表示倒数第2个元素

	//===========================
	name[2:-2] 切片操作
		print(name[2:-2]) thon语言程序设计
	
	
	/**
		语法：
		list[start: end:(step)],
			start表示起始位置，
			end表示结束位置
				//step表示步长，可以选择。
			start和end的选择为 左开右闭
	*/




